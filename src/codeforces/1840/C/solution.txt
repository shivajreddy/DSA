


k=3
q=12
12 12 10 15
1   1  0 1

sliding window: window size is k. window will hold if all are valid, by using 
1 or 0 for each position to be valid, if sum(window) == k, then all are valid
each iter remove left, add right
prefix: 

how many valid starting points

n.k
